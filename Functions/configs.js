//Process Parameters:
const TempMinAllowed = 10;
const TempMaxAllowed = 80;
const PressMinAllowed = 980;
const PressMaxAllowed = 1050;
export const Params = {
    TempMaxAllowed: TempMaxAllowed,
    TempMinAllowed: TempMinAllowed,
    PressMaxAllowed: PressMaxAllowed,
    PressMinAllowed: PressMinAllowed
};
